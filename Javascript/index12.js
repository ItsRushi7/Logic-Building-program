let calculatePower = (base, power) => base ** power;

function main() {
    let base = 2;
    let power = 3;
    let ans = 0;

    ans = calculatePower(value);

    console.log(`the power of ${value} is ${ans}`);
}

main()