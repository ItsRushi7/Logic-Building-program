function arrayFabbio(arr, size) {


    for (let index = 0; index < size; index++) {

        console.log(a);
        a = arr[index + 1];
        b = temp;
        temp = a + b;

    }
}
function main() {
    let arr = [10, 20, 30, 40, 50];
    let size = 5;

    // for (let count = 0; count < size; count++) {

    //     arr[count] = count;
    // }
    arrayFabbio(arr, size);
}
main();